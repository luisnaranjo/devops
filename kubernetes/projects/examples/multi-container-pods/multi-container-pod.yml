# Simple example of a multi-container pod.
# This pod consists of 3 containers.
apiVersion: v1
kind: Pod
metadata:
    name: multi-container-pod
spec:
    containers:
    - name: nginx
      image: nginx
    - name: redis
      image: redis
    - name: couchbase
      image: couchbase
